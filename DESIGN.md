# 闪记 FlashNote · 产品设计文档

> 一句话：白绿配色的卡片式快速记录工具，Mac + 微信小程序两端，局域网内自动同步，文字笔记与简单账目一套搞定。

---

## 1. 产品定位

### 1.1 一句话
把脑子里一闪而过的念头、和随手记的一笔小账，用最少的步骤沉淀下来，并在 Mac 和微信小程序之间无感流转。

### 1.2 核心场景
| 场景 | 描述 | 痛点 |
| --- | --- | --- |
| 路上记一笔 | 微信小程序随手开 → 写两句 / 记一笔 → 关掉就走 | 市面记账 App 启动慢、流程重 |
| 桌面整理 | Mac 端按时间/标签浏览卡片，给记录打标签、看月度小计 | 笔记软件不支持数字，记账软件不支持长文本 |
| 回家同步 | Mac 和小程序在同一个 WiFi 下，回到桌面一开 app 立刻齐 | 不想把数据交给云，也不想手动导入导出 |

### 1.3 设计原则
- **轻入口**：3 秒内进入记录，1 个输入框 + 2 个动作（存笔记 / 记一笔）。
- **卡片即正文**：每条记录都是一个独立卡片，可独立查看、编辑、分享。
- **数据本地优先**：所有内容都先落本地，同步是"加分项"不是"前置条件"。
- **绿色呼吸感**：白底 + 浅绿点缀，强调清新、不焦虑。

---

## 2. 功能列表

### 2.0 锁定的产品决策（2026-08-09）
- ❌ **不做收入**：只记录支出
- ❌ **不做多账户**：单一账本
- ✅ **同步坚持本地优先**：mDNS + 4 位配对码，不上云、不开端口暴露公网
- ✅ **同步策略保持简单**：能完成「两端数据最终一致」即可，复杂加密/冲突合并靠后

### 2.1 P0（首版必做）
- [ ] 快速记录：文字笔记（支持 #标签 / @分类）
- [ ] 快速记账：金额 + 分类（一句话内解析，例：`午饭 28 #餐`）
- [ ] 卡片流浏览：按时间倒序，标签/分类筛选
- [ ] 标签管理：自动从内容提取 + 手动维护
- [ ] 简单统计：本月支出 / 各分类小计（Mac 端）
- [ ] 局域网同步：自动发现 + 一键配对 + 增量同步
- [ ] 本地存储：完全离线可用

### 2.2 P1（迭代补）
- 搜索（全文 + 标签 + 金额范围）
- 编辑 / 删除卡片
- 卡片导出（Markdown / CSV）
- 月度账单回顾视图
- 自定义分类图标

### 2.3 P2（远期）
- iCloud / WebDAV 备份（仍坚持本地优先，云只是备份）
- 小组件（Mac 菜单栏 / 小程序桌面）
- 浅色 / 深色主题

---

## 3. 技术选型

| 端 | 技术 | 理由 |
| --- | --- | --- |
| Mac | **SwiftUI + AppKit** | 苹果原生，最佳 macOS 体验；Core Data 本地存储；Bonjour/mDNS 局域网发现开箱即用 |
| 微信小程序 | **微信原生 (TypeScript + WXML/WXSS)** | 无需解释；`wx.getLocalIPAddress`、`UDPSocket` 可做局域网发现 |
| 同步协议 | **mDNS 发现 + HTTP/WebSocket 传输 + JSON 数据** | 零依赖、易调试、可手动 curl 验证 |
| 数据格式 | **JSON + 本地数据库** | Mac 端用 Core Data / SQLite；小程序端用本地 storage（量小，迁移友好） |
| 包管理 | Mac: SwiftPM；小程序: npm | 不引入第三方框架，保持轻量 |

---

## 4. 数据模型

### 4.1 记录 Record
```json
{
  "id": "uuid-v4",
  "type": "note | expense",
  "content": "午饭吃了面",
  "amount": 28.0,            // 仅 expense
  "category": "餐",           // 仅 expense，可选
  "tags": ["餐", "工作日"],
  "createdAt": "2026-08-09T12:31:08+08:00",
  "updatedAt": "2026-08-09T12:31:08+08:00",
  "deviceId": "mac-xxx" | "mp-xxx",
  "deleted": false
}
```

### 4.2 标签 Tag
```json
{
  "id": "uuid-v4",
  "name": "餐",
  "color": "#52C41A",
  "count": 12,
  "createdAt": "..."
}
```

### 4.3 分类 Category（预置，仅支出方向）
- 餐 🍱 / 交通 🚇 / 购物 🛍 / 娱乐 🎮 / 居家 🏠 / 医疗 💊 / 其他 ✨
- 后续支持自定义

### 4.4 解析规则（一句话内同时承载笔记 + 记账）
- 数字 + 关键词（如 `28`、`28元`、`¥28`）→ 金额
- `#xxx` → 标签
- 剩余文本 → 笔记内容
- 例：`午饭 28 #餐 #工作日` → 笔记「午饭」，金额 28，标签「餐」「工作日」

---

## 5. 信息架构

### 5.1 Mac 端
```
闪记.app
├── 侧边栏
│   ├── 全部
│   ├── 笔记
│   ├── 账目
│   ├── 标签（自动聚合）
│   └── 统计
├── 主区
│   ├── 顶部：搜索栏 + 同步状态指示
│   ├── 卡片流（无限滚动，按月分组）
│   └── 底部：快速输入框（Cmd+Enter 提交，Shift 切类型）
└── 顶栏
    ├── 标题（当前筛选）
    ├── 同步设备列表
    └── 设置
```

### 5.2 微信小程序
```
闪记 小程序
├── 首页（默认）
│   ├── 顶部：搜索 + 同步状态
│   ├── 卡片流（时间分组）
│   └── 底部输入条
├── 统计 tab
│   ├── 本月支出大数字
│   ├── 分类环形图
│   └── 标签云
├── 我的 tab
│   ├── 设备配对
│   ├── 数据导出
│   └── 关于
└── 记录详情页（独立路由）
```

---

## 6. 视觉设计

### 6.1 色板
| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 主色 | `#52C41A`（豆绿） | 关键按钮、强调文本、活跃标签 |
| 主色浅 | `#F6FFED` | 标签 chip 底色、hover 背景 |
| 主色深 | `#389E0D` | 按钮按下、文字重点 |
| 背景 | `#FFFFFF` | 主背景 |
| 卡片底 | `#FFFFFF` + 阴影 `0 1px 2px rgba(0,0,0,0.04)` | 卡片本体 |
| 页面底 | `#FAFAFA` | 区分滚动区与卡片 |
| 文字主 | `#1F1F1F` | 标题 |
| 文字次 | `#595959` | 卡片内容 |
| 文字辅 | `#8C8C8C` | 时间戳、占位 |
| 分割 | `#F0F0F0` | 1px 浅线 |
| 警示 | `#FAAD14`（橙） | 删除确认、支出类高亮 |

### 6.2 字体
- Mac：`SF Pro Text` + `SF Pro Display`（数字与金额）
- 小程序：`PingFang SC`（系统字体）

### 6.3 间距与圆角
- 卡片圆角 `12px`
- 按钮圆角 `8px`
- 标签 chip `6px`、高度 22
- 卡片内边距 `16px`
- 卡片间距 `12px`
- 页面边距 `24px`（Mac）/ `16px`（小程序）

### 6.4 关键组件
- **快速输入框**：单行 / 多行可切换，焦点时变绿描边；右侧两个 tab：`📝 笔记` / `💰 账目`
- **卡片**：左侧 4px 绿色竖条标识类型（笔记=实线，账目=虚线 + 金额）
- **标签 chip**：浅绿底 + 绿字 + 圆角 6
- **同步状态点**：右上角小圆点，灰=未连，绿=已同步，橙=同步中

---

## 7. 关键交互

### 7.1 快速记录流程
1. 任意界面 `Cmd+N`（Mac）/ 点击底部「+」（小程序）
2. 弹起输入框，自动聚焦
3. 输入内容（实时解析高亮 `#tag` 与金额）
4. `Cmd+Enter` 提交，数字 1 抖动反馈后回到列表，卡片从顶部插入

### 7.2 局域网同步流程
1. 首次在 Mac 端开启「允许局域网同步」，绑定一个 4 位配对码
2. 微信小程序进入「我的 → 设备配对」，扫输入 4 位码
3. 配对后两端建立 mDNS + 长连接，后续自动同步
4. 同步策略：每 30s 增量同步一次（基于 updatedAt），手动「立即同步」可立即触发
5. 冲突解决：LWW（Last Write Wins），以 `updatedAt` 较新者为准

### 7.3 卡片详情
- 单击卡片 → 浮层（Mac）/ 独立页（小程序）
- 显示完整内容、标签、来源设备、同步时间
- 操作：编辑 / 删除 / 复制 / 分享为图片

---

## 8. 同步方案（局域网）

### 8.1 发现
- Mac 端启动 `Bonjour` 服务，service type `_flashnote._tcp`，端口 `9527`
- 小程序端用 `UDPSocket` 监听广播（端口 9527）→ 解析响应得到 host:port
- 备选：用户手动填写 Mac 的 IP

### 8.2 握手
1. 小程序 → Mac：`POST /api/pair`，body `{code, deviceName, deviceId}`
2. Mac 校验配对码 → 返回 `{token, serverInfo}`
3. 后续请求带 `Authorization: Bearer <token>`

### 8.3 增量同步
- `GET /api/records?since=<timestamp>&deviceId=<x>` → 返回该 deviceId 之后的所有变化
- `POST /api/records` 批量上传新记录
- 软删除通过 `deleted: true` 标记，30 天后清理

### 8.4 冲突与一致性
- 每条记录带 `id` (uuid) + `updatedAt`
- 同步时按 `updatedAt` 取最新，相同 `updatedAt` 视为同一条
- 离线编辑不阻塞：本地先存 → 联网时推送

### 8.5 安全
- 配对码 4 位 + token 持久化
- 局域网内明文传输（数据本就只走本地，敏感信息需自加密）
- 未来可加：用户自定义端口、配对码 6 位

---

## 9. 项目结构

```
FlashNote/
├── DESIGN.md                 ← 本文档
├── mac/                      ← Mac 端
│   ├── FlashNote.xcodeproj
│   ├── FlashNote/
│   │   ├── App/
│   │   ├── Views/
│   │   ├── Models/
│   │   ├── Store/            ← Core Data
│   │   ├── Sync/             ← mDNS + HTTP Server
│   │   └── Resources/
│   └── README.md
├── miniprogram/              ← 微信小程序
│   ├── miniprogram/
│   │   ├── pages/
│   │   ├── components/
│   │   ├── store/
│   │   ├── sync/
│   │   └── utils/
│   ├── app.json
│   └── README.md
├── protocol/                 ← 共享协议
│   ├── api.md
│   └── schema.json
└── prototype/                ← HTML 设计原型
    ├── mac.html
    └── miniprogram.html
```

---

## 10. 里程碑

| 阶段 | 内容 | 周期 | 状态 |
| --- | --- | --- | --- |
| M0 | 文档 + HTML 原型 | 当前 | ✅ |
| M1 | Mac 端 MVP（SwiftUI · 记录 · 本地存储 · 卡片流） | 2 周 | 🚧 进行中 |
| M2 | 微信小程序 MVP（记录 · 卡片流） | 1.5 周 | ⏳ |
| M3 | 局域网同步打通 | 2 周 | ⏳ |
| M4 | 统计、搜索、导出等 P1 | 2 周 | ⏳ |
| M5 | 优化、打磨、发布 | 1 周 | ⏳ |

---

## 11. 决策记录

| 决策点 | 锁定 | 日期 |
| --- | --- | --- |
| 收入/账户 | ❌ 不做收入、不做多账户，只单一支出账本 | 2026-08-09 |
| 同步策略 | mDNS + 4 位配对码 + 增量同步，**不上云** | 2026-08-09 |
| Mac 形态 | 原生 SwiftUI 单窗口 App | 2026-08-09 |
| 主色 | `#52C41A` 豆绿 | 2026-08-09 |
| 开发顺序 | M1 → M2 → M3 → M4 → M5 | 2026-08-09 |

---

> 设计 v0.1 定稿。下一步启动 M1：Mac 端 SwiftUI MVP（记录 + 本地存储 + 卡片流）。
